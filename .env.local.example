# 로컬 `vercel dev` 전용 (이 파일을 복사해 .env.local 로 쓰세요. Git에 올리지 마세요.)
# Vercel 대시보드에는 ADMIN_DEV_OTP_BYPASS 를 절대 넣지 마세요.

# 1 이면 localhost/127.0.0.1 에서만 OTP 생략 (배포 alpha/real 에는 영향 없음)
ADMIN_DEV_OTP_BYPASS=1
